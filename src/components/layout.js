import { escapeHtml } from "../utils/html.js";
const titles={dashboard:"프로젝트 흐름을 한눈에",workflows:"Workflow 설계",runs:"실행 현황"};
export function renderLayout(state,content){const nav=[["dashboard","개요","◫"],["workflows","Workflow 설계","◇"],["runs","실행 현황","▶"]];return `<div class="shell"><aside class="sidebar"><div class="brand"><span class="brand-mark">F</span><div><strong>Flowline</strong><small>ETVX workflow</small></div></div><nav>${nav.map(([v,l,i])=>`<button class="nav-item ${state.activeView===v?"active":""}" data-action="navigate" data-view="${v}"><span>${i}</span>${l}</button>`).join("")}</nav><div class="sidebar-note"><strong>Flask + SQLite</strong><span>Workflow와 좌표를 서버에 저장합니다.</span></div></aside><main class="main"><header class="topbar"><div><span class="eyebrow">PROJECT OPERATIONS</span><h1>${escapeHtml(titles[state.activeView])}</h1></div><div class="avatar">운영</div></header>${content}</main><div id="toast" class="toast"></div></div>`}
